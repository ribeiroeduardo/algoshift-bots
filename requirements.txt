# Root file so Nixpacks installs Python (repo has package.json; worker deps live here too).
# supabase 2.3.x + loose gotrue → TypeError: Client.__init__() got an unexpected keyword argument 'proxy'
supabase==2.28.3
ccxt==4.1.80
python-dotenv==1.0.0
